# 校園舊書箱 (Campus Old Book Box) - 系統功能說明文件

這份文件彙整了目前「校園舊書箱」專案中所有已實作的核心功能、系統架構以及資料模型狀態。

## 1. 使用者與權限管理
系統主要分為兩種角色 (Role)：**一般學生 (STUDENT)** 與 **管理員 (ADMIN)**。

*   **登入系統 (NextAuth)**：
    *   提供 OAuth 登入 (支援 Google 登入)。
    *   整合 LINE 登入/綁定：當使用者透過 LINE 官方帳號互動時，會自動建立帳號 (Auto-provisioning)，並自動串接 LINE Profile API 取得使用者的真實 LINE 暱稱。
*   **權限控制**：
    *   一般學生只能進行捐書、瀏覽書籍、預約及領取書籍。
    *   管理員可以進入後台 (`/admin`) 管理書籍狀態、審核捐書，並接收 LINE 的審核通知。

## 2. 捐書與審核流程 (Donate & Review)
*   **學生捐書 (`/donate`)**：
    *   學生可以填寫書籍資訊 (ISBN、書名、書況描述) 並上傳書籍照片。
    *   圖片上傳整合了 **Cloudflare R2** 儲存服務。
    *   捐贈後的書籍狀態會設為 `PENDING` (待審核)。
*   **管理員審核 (LINE Bot 整合)**：
    *   當有新捐書時，系統會主動推播 (Multicast) **LINE Flex Message (圖文選單卡片)** 給所有管理員。
    *   管理員可以直接在 LINE 聊天室中點擊「✅ 核准上架」或「❌ 退回」(透過 Postback Action 觸發 Webhook)。
    *   核准後，書籍狀態會更新為 `IN_LOCKER` (庫存中)；退回則變更為 `REJECTED`。

## 3. 書籍預約與領取 (Browse, Reserve & Claim)
*   **書籍瀏覽 (`/browse`)**：
    *   學生可以瀏覽目前在書箱內 (狀態為 `IN_LOCKER`) 的所有書籍清單。
*   **預約功能**：
    *   學生看到喜歡的書可以進行預約，書籍狀態變更為 `RESERVED` (已預約)，防止其他學生重複預約。
*   **領取功能 (`/claim`)**：
    *   學生前往實體書箱 (例如：系辦走廊)，並透過系統完成領取程序。
    *   領取成功後，狀態變更為 `CLAIMED` (已領取)。
    *   （未來擴充：針對保留過久未領取的書籍，狀態會變更為 `EXPIRED` 進行二次分流處理）。

## 4. 實體書箱管理 (Locker Management)
*   紀錄實體書箱的狀態 (`LockerStatus`)，包含：
    *   放置地點 (Location)。
    *   書箱容量 (Capacity) 與 當前負載 (CurrentLoad)。
    *   書箱密碼管理 (MVP 階段預設為固定密碼)。

## 5. 系統日誌與交易紀錄 (Transactions)
*   系統會記錄所有的核心動作 (`Transaction`)，包含：
    *   `DONATE` (捐書)
    *   `RESERVE` (預約)
    *   `CLAIM` (領取)
*   每一筆交易都會綁定對應的使用者 (User) 與書籍 (Book)，並可附加備註 (如感謝語)。

## 6. 技術堆疊與外部服務 (Tech Stack)
*   **核心框架**：Next.js 14/15 (App Router), React, TypeScript
*   **資料庫與 ORM**：Prisma ORM + Neon Postgres (Serverless Database)
*   **身分驗證**：NextAuth.js
*   **圖片儲存**：Cloudflare R2 (S3 API 相容)
*   **通訊與機器人**：LINE Messaging API (Bot SDK)
*   **AI 整合**：Gemini AI API (預留於 `.env` 中，可能用於圖片辨識或自動帶入 ISBN 書籍資訊)
